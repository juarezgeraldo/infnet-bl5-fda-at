<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.infnet_bl5_fda_at">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icone_inicial"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icone_inicial"
        android:supportsRtl="true"
        android:theme="@style/Theme.Infnetbl5fdaat"
        tools:targetApi="31">
        <activity
            android:name="com.example.infnet_bl5_fda_at.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Infnetbl5fdaat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>